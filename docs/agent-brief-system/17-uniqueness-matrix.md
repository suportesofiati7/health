# Concept Uniqueness Matrix

This matrix summarizes each concept's protected identity and remaining design risk. It is generated from concept planning files, partial classes, screenshots and the issue register.

| Concept | Status | Brief Confidence | Similarity / Risk Notes | Protected Fix Boundary |
| --- | --- | --- | --- | --- |
| 01-inspire | high risk | high | compare closely against concepts `02` and `09` during duplicate-layout audits.; active issues: C01-RENDER-001, C01-RENDER-002 | It is identified by `pressed botanical editorial`, portrait treatment `arched ivory portrait with bronze pressed-leaf edge`, motion `line reveal`, form `minimal ivory form` and footer `business-card footer`. The HTML, CSS and JS include unique markers for this concept. |
| 02-empower | needs review | high | compare closely against concepts `03` and `10` during duplicate-layout audits.; active issues: C02-RENDER-001 | It is identified by `business-card seal system`, portrait treatment `round monogram-backed portrait stamp`, motion `leaf orbit`, form `sage panel form` and footer `large monogram ledger`. The HTML, CSS and JS include unique markers for this concept. |
| 03-enhance | high risk | high | compare closely against concepts `04` and `11` during duplicate-layout audits.; active issues: C03-LANG-001, C03-FOOTER-001, C03-RENDER-001 | It is identified by `clinical magazine linework`, portrait treatment `sage duotone editorial profile tile`, motion `portrait lift`, form `botanical framed form` and footer `signature seal footer`. The HTML, CSS and JS include unique markers for this concept. |
| 04-renew | strong | high | compare closely against concepts `05` and `12` during duplicate-layout audits.; active issues: none | It is identified by `laser dossier geometry`, portrait treatment `business-card portrait with signature line`, motion `icon tilt`, form `gold line form` and footer `clinical column footer`. The HTML, CSS and JS include unique markers for this concept. |
| 05-elevate | high risk | high | compare closely against concepts `06` and `13` during duplicate-layout audits.; active issues: C05-HEADER-001, C05-FOOTER-001, C05-RENDER-001 | It is identified by `quiet luxury watermark`, portrait treatment `full-height consultation portrait badge`, motion `gold shimmer`, form `step-style consultation form` and footer `botanical map footer`. The HTML, CSS and JS include unique markers for this concept. |
| 06-refine | high risk | high | compare closely against concepts `07` and `14` during duplicate-layout audits.; active issues: C06-NAV-001, C06-RENDER-001, C06-RENDER-002 | It is identified by `mobile story botanical path`, portrait treatment `soft oval botanical frame`, motion `menu bloom`, form `sidebar form` and footer `concierge contact footer`. The HTML, CSS and JS include unique markers for this concept. |
| 07-glow | high risk | high | compare closely against concepts `08` and `15` during duplicate-layout audits.; active issues: C07-RENDER-001, C07-RENDER-002 | It is identified by `proof-grid bronze markers`, portrait treatment `clinical credential portrait card`, motion `divider trace`, form `floating editorial form` and footer `apothecary fine-print footer`. The HTML, CSS and JS include unique markers for this concept. |
| 08-balance | strong | high | compare closely against concepts `09` and `16` during duplicate-layout audits.; active issues: none | It is identified by `round monogram sanctuary`, portrait treatment `champagne line portrait seal`, motion `form focus glow`, form `business-card form` and footer `bronze strip footer`. The HTML, CSS and JS include unique markers for this concept. |
| 09-radiance | needs review | high | compare closely against concepts `10` and `17` during duplicate-layout audits.; active issues: C09-RENDER-001 | It is identified by `consultation studio badges`, portrait treatment `mobile avatar with FS watermark`, motion `journal card rise`, form `mobile-first bottom form` and footer `story-background footer`. The HTML, CSS and JS include unique markers for this concept. |
| 10-essence | high risk | high | compare closely against concepts `11` and `18` during duplicate-layout audits.; active issues: C10-HEADER-001, C10-RENDER-001, C10-RENDER-002 | It is identified by `minimal appointment stationery`, portrait treatment `journal-side portrait notecard`, motion `service badge pulse`, form `portrait-led consultation form` and footer `credential stamp footer`. The HTML, CSS and JS include unique markers for this concept. |
| 11-bloom | high risk | high | compare closely against concepts `12` and `19` during duplicate-layout audits.; active issues: C11-DECOR-001, C11-RENDER-001 | It is identified by `institutional credential atlas`, portrait treatment `arched ivory portrait with bronze pressed-leaf edge`, motion `line reveal`, form `minimal ivory form` and footer `business-card footer`. The HTML, CSS and JS include unique markers for this concept. |
| 12-vital | needs review | high | compare closely against concepts `13` and `20` during duplicate-layout audits.; active issues: C12-RENDER-001 | It is identified by `dermatology portal pictograms`, portrait treatment `round monogram-backed portrait stamp`, motion `leaf orbit`, form `sage panel form` and footer `large monogram ledger`. The HTML, CSS and JS include unique markers for this concept. |
| 13-poise | high risk | high | compare closely against concepts `14` and `21` during duplicate-layout audits.; active issues: C13-FOOTER-001, C13-RENDER-001 | It is identified by `private column frames`, portrait treatment `sage duotone editorial profile tile`, motion `portrait lift`, form `botanical framed form` and footer `signature seal footer`. The HTML, CSS and JS include unique markers for this concept. |
| 14-aura | strong | high | compare closely against concepts `15` and `22` during duplicate-layout audits.; active issues: none | It is identified by `champagne ritual overlays`, portrait treatment `business-card portrait with signature line`, motion `icon tilt`, form `gold line form` and footer `clinical column footer`. The HTML, CSS and JS include unique markers for this concept. |
| 15-clarity | high risk | high | compare closely against concepts `16` and `23` during duplicate-layout audits.; active issues: C15-FOOTER-001, C15-RENDER-001 | It is identified by `procedure atlas symbols`, portrait treatment `full-height consultation portrait badge`, motion `gold shimmer`, form `step-style consultation form` and footer `botanical map footer`. The HTML, CSS and JS include unique markers for this concept. |
| 16-grace | high risk | high | compare closely against concepts `17` and `24` during duplicate-layout audits.; active issues: C16-NAV-001, C16-RENDER-001 | It is identified by `service shelf labels`, portrait treatment `soft oval botanical frame`, motion `menu bloom`, form `sidebar form` and footer `concierge contact footer`. The HTML, CSS and JS include unique markers for this concept. |
| 17-sculpt | needs review | high | compare closely against concepts `18` and `25` during duplicate-layout audits.; active issues: C17-MOBILE-001, C17-RENDER-001 | It is identified by `local branch contact marks`, portrait treatment `clinical credential portrait card`, motion `divider trace`, form `floating editorial form` and footer `apothecary fine-print footer`. The HTML, CSS and JS include unique markers for this concept. |
| 18-lumin | high risk | high | compare closely against concepts `19` and `26` during duplicate-layout audits.; active issues: C18-FOOTER-001, C18-RENDER-001 | It is identified by `high-contrast clinic strokes`, portrait treatment `champagne line portrait seal`, motion `form focus glow`, form `business-card form` and footer `bronze strip footer`. The HTML, CSS and JS include unique markers for this concept. |
| 19-verda | needs review | high | compare closely against concepts `20` and `27` during duplicate-layout audits.; active issues: C19-RENDER-001 | It is identified by `local search direction tags`, portrait treatment `mobile avatar with FS watermark`, motion `journal card rise`, form `mobile-first bottom form` and footer `story-background footer`. The HTML, CSS and JS include unique markers for this concept. |
| 20-halo | strong | high | compare closely against concepts `21` and `28` during duplicate-layout audits.; active issues: none | It is identified by `physician profile stamp`, portrait treatment `journal-side portrait notecard`, motion `service badge pulse`, form `portrait-led consultation form` and footer `credential stamp footer`. The HTML, CSS and JS include unique markers for this concept. |
| 21-calm | needs review | high | compare closely against concepts `22` and `29` during duplicate-layout audits.; active issues: C21-RENDER-001 | It is identified by `concierge clinic panels`, portrait treatment `arched ivory portrait with bronze pressed-leaf edge`, motion `line reveal`, form `minimal ivory form` and footer `business-card footer`. The HTML, CSS and JS include unique markers for this concept. |
| 22-precision | high risk | high | compare closely against concepts `23` and `30` during duplicate-layout audits.; active issues: C22-RENDER-001, C22-RENDER-002 | It is identified by `signature consultant trace`, portrait treatment `round monogram-backed portrait stamp`, motion `leaf orbit`, form `sage panel form` and footer `large monogram ledger`. The HTML, CSS and JS include unique markers for this concept. |
| 23-ritual | needs review | high | compare closely against concepts `24` and `31` during duplicate-layout audits.; active issues: C23-RENDER-001 | It is identified by `medical skin disclosure set`, portrait treatment `sage duotone editorial profile tile`, motion `portrait lift`, form `botanical framed form` and footer `signature seal footer`. The HTML, CSS and JS include unique markers for this concept. |
| 24-signal | needs review | high | compare closely against concepts `25` and `32` during duplicate-layout audits.; active issues: C24-RENDER-001 | It is identified by `fine-list micro glyphs`, portrait treatment `business-card portrait with signature line`, motion `icon tilt`, form `gold line form` and footer `clinical column footer`. The HTML, CSS and JS include unique markers for this concept. |
| 25-align | needs review | high | compare closely against concepts `26` and `33` during duplicate-layout audits.; active issues: C25-RENDER-001 | It is identified by `treatment explorer chips`, portrait treatment `full-height consultation portrait badge`, motion `gold shimmer`, form `step-style consultation form` and footer `botanical map footer`. The HTML, CSS and JS include unique markers for this concept. |
| 26-vivant | needs review | high | compare closely against concepts `27` and `34` during duplicate-layout audits.; active issues: C26-RENDER-001 | It is identified by `editorial authority plaques`, portrait treatment `soft oval botanical frame`, motion `menu bloom`, form `sidebar form` and footer `concierge contact footer`. The HTML, CSS and JS include unique markers for this concept. |
| 27-form | high risk | high | compare closely against concepts `28` and `35` during duplicate-layout audits.; active issues: C27-LOGO-001, C27-RENDER-001 | It is identified by `procedure library tabs`, portrait treatment `clinical credential portrait card`, motion `divider trace`, form `floating editorial form` and footer `apothecary fine-print footer`. The HTML, CSS and JS include unique markers for this concept. |
| 28-pure | high risk | high | compare closely against concepts `29` and `36` during duplicate-layout audits.; active issues: C28-LANG-001, C28-FOOTER-001, C28-RENDER-001, C28-RENDER-002 | It is identified by `single-service education marks`, portrait treatment `champagne line portrait seal`, motion `form focus glow`, form `business-card form` and footer `bronze strip footer`. The HTML, CSS and JS include unique markers for this concept. |
| 29-solace | needs review | high | compare closely against concepts `30` and `37` during duplicate-layout audits.; active issues: C29-RENDER-001 | It is identified by `doctor journal notecards`, portrait treatment `mobile avatar with FS watermark`, motion `journal card rise`, form `mobile-first bottom form` and footer `story-background footer`. The HTML, CSS and JS include unique markers for this concept. |
| 30-method | high risk | high | compare closely against concepts `31` and `38` during duplicate-layout audits.; active issues: C30-NAV-001, C30-RENDER-001, C30-RENDER-002 | It is identified by `booking concierge steps`, portrait treatment `journal-side portrait notecard`, motion `service badge pulse`, form `portrait-led consultation form` and footer `credential stamp footer`. The HTML, CSS and JS include unique markers for this concept. |
| 31-evolve | high risk | high | compare closely against concepts `32` and `39` during duplicate-layout audits.; active issues: C31-RENDER-001, C31-RENDER-002 | It is identified by `confidence timeline leaves`, portrait treatment `arched ivory portrait with bronze pressed-leaf edge`, motion `line reveal`, form `minimal ivory form` and footer `business-card footer`. The HTML, CSS and JS include unique markers for this concept. |
| 32-serene | strong | high | compare closely against concepts `33` and `40` during duplicate-layout audits.; active issues: none | It is identified by `natural result privacy frames`, portrait treatment `round monogram-backed portrait stamp`, motion `leaf orbit`, form `sage panel form` and footer `large monogram ledger`. The HTML, CSS and JS include unique markers for this concept. |
| 33-elan | needs review | high | compare closely against concepts `34` and `41` during duplicate-layout audits.; active issues: C33-RENDER-001 | It is identified by `destination journey pins`, portrait treatment `sage duotone editorial profile tile`, motion `portrait lift`, form `botanical framed form` and footer `signature seal footer`. The HTML, CSS and JS include unique markers for this concept. |
| 34-flora | needs review | high | compare closely against concepts `35` and `42` during duplicate-layout audits.; active issues: C34-RENDER-001 | It is identified by `artisan botanical craft`, portrait treatment `business-card portrait with signature line`, motion `icon tilt`, form `gold line form` and footer `clinical column footer`. The HTML, CSS and JS include unique markers for this concept. |
| 35-atelier | needs review | high | compare closely against concepts `36` and `43` during duplicate-layout audits.; active issues: C35-RENDER-001 | It is identified by `laser studio membership passes`, portrait treatment `full-height consultation portrait badge`, motion `gold shimmer`, form `step-style consultation form` and footer `botanical map footer`. The HTML, CSS and JS include unique markers for this concept. |
| 36-lumina | needs review | high | compare closely against concepts `37` and `44` during duplicate-layout audits.; active issues: C36-RENDER-001 | It is identified by `skincare bar progress marks`, portrait treatment `soft oval botanical frame`, motion `menu bloom`, form `sidebar form` and footer `concierge contact footer`. The HTML, CSS and JS include unique markers for this concept. |
| 37-vellum | needs review | high | compare closely against concepts `38` and `45` during duplicate-layout audits.; active issues: C37-RENDER-001 | It is identified by `treatment pass slots`, portrait treatment `clinical credential portrait card`, motion `divider trace`, form `floating editorial form` and footer `apothecary fine-print footer`. The HTML, CSS and JS include unique markers for this concept. |
| 38-origin | needs review | high | compare closely against concepts `39` and `46` during duplicate-layout audits.; active issues: C38-RENDER-001 | It is identified by `doctor shop editorial labels`, portrait treatment `champagne line portrait seal`, motion `form focus glow`, form `business-card form` and footer `bronze strip footer`. The HTML, CSS and JS include unique markers for this concept. |
| 39-kindred | needs review | high | compare closely against concepts `40` and `47` during duplicate-layout audits.; active issues: C39-RENDER-001 | It is identified by `science ingredient diagrams`, portrait treatment `mobile avatar with FS watermark`, motion `journal card rise`, form `mobile-first bottom form` and footer `story-background footer`. The HTML, CSS and JS include unique markers for this concept. |
| 40-noble | strong | high | compare closely against concepts `41` and `48` during duplicate-layout audits.; active issues: none | It is identified by `prestige formula shimmer`, portrait treatment `journal-side portrait notecard`, motion `service badge pulse`, form `portrait-led consultation form` and footer `credential stamp footer`. The HTML, CSS and JS include unique markers for this concept. |
| 41-vista | needs review | high | compare closely against concepts `42` and `49` during duplicate-layout audits.; active issues: C41-RENDER-001 | It is identified by `ritual story seals`, portrait treatment `arched ivory portrait with bronze pressed-leaf edge`, motion `line reveal`, form `minimal ivory form` and footer `business-card footer`. The HTML, CSS and JS include unique markers for this concept. |
| 42-softline | strong | high | compare closely against concepts `43` and `50` during duplicate-layout audits.; active issues: none | It is identified by `apothecary text stamps`, portrait treatment `round monogram-backed portrait stamp`, motion `leaf orbit`, form `sage panel form` and footer `large monogram ledger`. The HTML, CSS and JS include unique markers for this concept. |
| 43-meridian | needs review | high | compare closely against concepts `44` and `01` during duplicate-layout audits.; active issues: C43-RENDER-001 | It is identified by `maison collection crests`, portrait treatment `sage duotone editorial profile tile`, motion `portrait lift`, form `botanical framed form` and footer `signature seal footer`. The HTML, CSS and JS include unique markers for this concept. |
| 44-safeguard | needs review | high | compare closely against concepts `45` and `02` during duplicate-layout audits.; active issues: C44-RENDER-001 | It is identified by `cream commerce labels`, portrait treatment `business-card portrait with signature line`, motion `icon tilt`, form `gold line form` and footer `clinical column footer`. The HTML, CSS and JS include unique markers for this concept. |
| 45-silhouette | needs review | high | compare closely against concepts `46` and `03` during duplicate-layout audits.; active issues: C45-RENDER-001 | It is identified by `clinical drop capsules`, portrait treatment `full-height consultation portrait badge`, motion `gold shimmer`, form `step-style consultation form` and footer `botanical map footer`. The HTML, CSS and JS include unique markers for this concept. |
| 46-curate | high risk | high | compare closely against concepts `47` and `04` during duplicate-layout audits.; active issues: C46-NAV-001, C46-RENDER-001, C46-RENDER-002 | It is identified by `spa menu botanical tabs`, portrait treatment `soft oval botanical frame`, motion `menu bloom`, form `sidebar form` and footer `concierge contact footer`. The HTML, CSS and JS include unique markers for this concept. |
| 47-proof | needs review | high | compare closely against concepts `48` and `05` during duplicate-layout audits.; active issues: C47-RENDER-001 | It is identified by `expert routine sequence`, portrait treatment `clinical credential portrait card`, motion `divider trace`, form `floating editorial form` and footer `apothecary fine-print footer`. The HTML, CSS and JS include unique markers for this concept. |
| 48-signature | strong | high | compare closely against concepts `49` and `06` during duplicate-layout audits.; active issues: none | It is identified by `botanical serum line growth`, portrait treatment `champagne line portrait seal`, motion `form focus glow`, form `business-card form` and footer `bronze strip footer`. The HTML, CSS and JS include unique markers for this concept. |
| 49-wisdom | needs review | high | compare closely against concepts `50` and `07` during duplicate-layout audits.; active issues: C49-RENDER-001 | It is identified by `farm-clean field notes`, portrait treatment `mobile avatar with FS watermark`, motion `journal card rise`, form `mobile-first bottom form` and footer `story-background footer`. The HTML, CSS and JS include unique markers for this concept. |
| 50-sovereign | strong | high | compare closely against concepts `01` and `08` during duplicate-layout audits.; active issues: none | It is identified by `ingredient lab minimal grid`, portrait treatment `journal-side portrait notecard`, motion `service badge pulse`, form `portrait-led consultation form` and footer `credential stamp footer`. The HTML, CSS and JS include unique markers for this concept. |
